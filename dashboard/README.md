# Shorts Engine Dashboard

This directory contains the optional dashboard foundation for Shorts Engine.

- `backend/`: lightweight API server for overview, analytics, settings, and cost endpoints
- `frontend/`: Next.js scaffold for dashboard pages and UI components

Run the backend with:

```bash
npm run build
node dist/src/cli/index.js dashboard
```
